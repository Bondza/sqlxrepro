CREATE TABLE a (
    id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    date DATE NOT NULL
);
CREATE TABLE b (
    id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    a_id BIGINT NOT NULL REFERENCES a (id),
    name TEXT
);